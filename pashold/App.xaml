<Application x:Class="pashold.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:pashold"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Discord-inspired palette -->
        <Color x:Key="DiscordBlurpleColor">#5865F2</Color>
        <Color x:Key="DiscordBlurpleHoverColor">#4752C4</Color>
        <Color x:Key="DiscordGreenColor">#57F287</Color>
        <!--<Color x:Key="DiscordRedColor">#ED4245</Color>-->
        <Color x:Key="DiscordRedColor">#A3282B</Color>
        <Color x:Key="DiscordDarkColor">#36393F</Color>
        <Color x:Key="DiscordDarkerColor">#2F3136</Color>
        <Color x:Key="DiscordDarkestColor">#202225</Color>
        <Color x:Key="DiscordElevatedColor">#40444B</Color>
        <Color x:Key="DiscordInputColor">#292B2F</Color>
        <Color x:Key="DiscordTextColor">#F2F3F5</Color>
        <Color x:Key="DiscordMutedTextColor">#B9BBBE</Color>
        <Color x:Key="DiscordBorderColor">#4F545C</Color>

        <SolidColorBrush x:Key="DiscordBlurpleBrush" Color="{StaticResource DiscordBlurpleColor}"/>
        <SolidColorBrush x:Key="DiscordBlurpleHoverBrush" Color="{StaticResource DiscordBlurpleHoverColor}"/>
        <SolidColorBrush x:Key="DiscordGreenBrush" Color="{StaticResource DiscordGreenColor}"/>
        <SolidColorBrush x:Key="DiscordRedBrush" Color="{StaticResource DiscordRedColor}"/>
        <SolidColorBrush x:Key="DiscordDarkBrush" Color="{StaticResource DiscordDarkColor}"/>
        <SolidColorBrush x:Key="DiscordDarkerBrush" Color="{StaticResource DiscordDarkerColor}"/>
        <SolidColorBrush x:Key="DiscordDarkestBrush" Color="{StaticResource DiscordDarkestColor}"/>
        <SolidColorBrush x:Key="DiscordElevatedBrush" Color="{StaticResource DiscordElevatedColor}"/>
        <SolidColorBrush x:Key="DiscordInputBrush" Color="{StaticResource DiscordInputColor}"/>
        <SolidColorBrush x:Key="DiscordTextBrush" Color="{StaticResource DiscordTextColor}"/>
        <SolidColorBrush x:Key="DiscordMutedTextBrush" Color="{StaticResource DiscordMutedTextColor}"/>
        <SolidColorBrush x:Key="DiscordBorderBrush" Color="{StaticResource DiscordBorderColor}"/>

        <Style TargetType="Window">
            <Setter Property="Background" Value="{StaticResource DiscordDarkBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource DiscordTextBrush}"/>
            <Setter Property="FontFamily" Value="Segoe UI, Arial"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource DiscordTextBrush}"/>
            <Setter Property="FontFamily" Value="Segoe UI, Arial"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource DiscordBlurpleBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,7"/>
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="FontFamily" Value="Segoe UI Semibold, Segoe UI, Arial"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource DiscordBlurpleHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource DiscordDarkestBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource DiscordRedBrush}"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource DiscordInputBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource DiscordTextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DiscordBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="9,6"/>
            <Setter Property="CaretBrush" Value="{StaticResource DiscordTextBrush}"/>
            <Setter Property="SelectionBrush" Value="{StaticResource DiscordBlurpleBrush}"/>
            <Setter Property="FontFamily" Value="Segoe UI, Arial"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="InputBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="7">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="InputBorder" Property="BorderBrush" Value="{StaticResource DiscordBlurpleBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="{StaticResource DiscordInputBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource DiscordTextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DiscordBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="9,6"/>
            <Setter Property="CaretBrush" Value="{StaticResource DiscordTextBrush}"/>
            <Setter Property="SelectionBrush" Value="{StaticResource DiscordBlurpleBrush}"/>
            <Setter Property="FontFamily" Value="Segoe UI, Arial"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="InputBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="7">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="InputBorder" Property="BorderBrush" Value="{StaticResource DiscordBlurpleBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="{StaticResource DiscordDarkerBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource DiscordTextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DiscordBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="4,3"/>
            <Setter Property="Foreground" Value="{StaticResource DiscordTextBrush}"/>
        </Style>

        <Style TargetType="ProgressBar">
            <Setter Property="Foreground" Value="{StaticResource DiscordGreenBrush}"/>
            <Setter Property="Background" Value="{StaticResource DiscordDarkestBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid ClipToBounds="True" Background="{TemplateBinding Background}">
                            <Rectangle x:Name="PART_Track" Fill="{TemplateBinding Background}"/>
                            <Decorator x:Name="PART_Indicator" HorizontalAlignment="Left">
                                <Rectangle Fill="{TemplateBinding Foreground}"/>
                            </Decorator>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>